# Core
fastapi>=0.111
uvicorn[standard]>=0.30
pydantic>=2.7
python-dotenv>=1.0
httpx>=0.27
tenacity>=8.3
asyncio-throttle>=1.0.2
pandas>=2.2

# LLM / Embeddings
openai>=1.35
anthropic>=0.30

# PDF parsing
docling>=2.0          # layout-aware table extraction (Milestone streams A/B)
pypdf>=4.2            # lightweight text extraction (Stream C literature)

# Vector DB (pick one — both included so VECTOR_BACKEND is a pure .env switch)
pinecone>=4.0
weaviate-client>=4.6

# Hybrid search
rank-bm25>=0.2.2
sentence-transformers>=3.0   # cross-encoder reranker
# cohere>=5.5                # optional: uncomment if RERANKER_BACKEND=cohere

# Dev
pytest>=8.2
